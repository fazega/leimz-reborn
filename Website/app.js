const pages=[...document.querySelectorAll('[data-page]')];
function route(){const name=location.hash.slice(1)||'accueil';const valid=pages.some(p=>p.dataset.page===name)?name:'accueil';pages.forEach(p=>p.hidden=p.dataset.page!==valid);document.querySelectorAll('nav a').forEach(a=>{const active=a.dataset.route===valid;a.classList.toggle('active',active);if(active)a.setAttribute('aria-current','page');else a.removeAttribute('aria-current')});document.title=`Leïmz Online — ${({accueil:'Le carnet des aventuriers',univers:'Le jeu',galerie:'Médias',communaute:'Les forums',faq:'F.A.Q.'})[valid]}`}
window.addEventListener('hashchange',route);route();
const island=document.querySelector('.island');for(let y=0;y<5;y++){for(let x=0;x<5;x++){if((x===0&&y===0)||(x===4&&y===4))continue;const tile=document.createElement('img');tile.src=(x===2||y===2)?'assets/stone.png':'assets/grass.png';tile.alt='';tile.style.left=`${(x-y)*25+88}px`;tile.style.top=`${(x+y)*13+27}px`;tile.style.zIndex=String(x+y);island.append(tile)}}
const dialog=document.querySelector('#lightbox'),image=dialog.querySelector('img'),caption=dialog.querySelector('p');const cards=[...document.querySelectorAll('.gallery-card')];let current=0;function show(src,title){image.src=src;image.alt=title;caption.textContent=title;current=cards.findIndex(c=>c.dataset.open===src);if(!dialog.open)dialog.showModal()}document.querySelectorAll('[data-open]').forEach(b=>b.addEventListener('click',()=>show(b.dataset.open,b.dataset.title)));dialog.querySelector('.close').onclick=()=>dialog.close();dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});function move(n){current=(current+n+cards.length)%cards.length;show(cards[current].dataset.open,cards[current].dataset.title)}document.querySelector('#previous').onclick=()=>move(-1);document.querySelector('#next').onclick=()=>move(1);document.addEventListener('keydown',e=>{if(dialog.open&&e.key==='ArrowRight')move(1);if(dialog.open&&e.key==='ArrowLeft')move(-1)});function filter(kind){document.querySelectorAll('[data-filter]').forEach(x=>{x.classList.toggle('selected',x.dataset.filter===kind);x.setAttribute('aria-pressed',String(x.dataset.filter===kind))});cards.forEach(c=>c.hidden=kind!=='all'&&kind!==c.dataset.kind)}document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>filter(b.dataset.filter));document.querySelectorAll('[data-route="galerie"]').forEach(a=>a.addEventListener('click',()=>filter(a.dataset.filterLink||'all')));
